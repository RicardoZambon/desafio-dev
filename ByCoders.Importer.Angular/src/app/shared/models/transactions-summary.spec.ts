import { TransactionsSummary } from './transactions-summary';

describe('TransactionsSummary', () => {
  it('should create an instance', () => {
    expect(new TransactionsSummary()).toBeTruthy();
  });
});
